import { JsonSchemaValidators } from './types';
/** This simply memoizes the result to prevent over fetching */
export declare class AugmentedSchemaValidators implements JsonSchemaValidators {
    private schemaValidators;
    constructor(schemaValidators: JsonSchemaValidators);
    isAugmented: boolean;
    validateSectionSchema: import("./utils").MemoedFunction<() => Promise<import("./types").ValidateFunction<unknown>>>;
}
